<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Waddl"
        tools:targetApi="31">
        <activity
trinity_branch
            android:name=".ui.welcome.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

            android:name=".ui.taskdeletedactivity.TaskUpdatedActivity"
            android:exported="false" />
main
        <activity
            android:name=".ui.updatetaskactivity.UpdateTaskActivity"
            android:exported="false" />
        <activity
            android:name=".ui.addtask.AddTaskActivity"
            android:exported="false" />
        <activity
            android:name=".ui.secretactivity.SecretActivity"
            android:exported="false" />
        <activity
            android:name=".ui.mainactivity.MainActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>